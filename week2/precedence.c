#include<stdio.h>

int main(){
    int x = 1 + 2 * (3 -1);
    printf("the value of x is %d\n",x);
    int y = 1 + 2 / 4 - 1 * 8 + 9 - 9 * (8+1);
    printf("the value of y is %d\n",y);
    return 0;
}
